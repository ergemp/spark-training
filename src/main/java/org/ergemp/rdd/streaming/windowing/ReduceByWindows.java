package org.ergemp.rdd.streaming.windowing;

public class ReduceByWindows {
    public static void main(String[] args) {
        // reduceByWindow(func, windowLength, slideInterval)
        // Return a new single-element stream,
        // created by aggregating elements in the stream over a sliding interval using func.
        //
        // The function should be associative and commutative
        // so that it can be computed correctly in parallel.
    }
}
